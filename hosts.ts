/**
 * A module that contains the string "hosts".
 * 
 * @example Logging the string "hosts":
 * ```ts
 * import hosts from "@etc/hosts";
 * console.log(hosts);
 * ```
 * 
 * @module
 */


const hosts = "hosts";

export default hosts;